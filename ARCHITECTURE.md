# Архитектура CKS v1.2

## Назначение

CKS (Context Knowledge System — система контекста и знаний) является независимой системой управления контекстом, знаниями, решениями, доказательствами и историей изменений.

> Этот документ описывает замороженный архитектурный baseline/Core v1.2. Развиваемый рабочий слой описывается отдельно в `control/system-state.yaml` и `docs/CKS_CURRENT_WORKING_STATE_SNAPSHOT_2026-09-17.md`. Наличие более нового Runtime (рабочего слоя) не означает автоматического изменения Canon или разморозки Core.

## Граница системы

CKS не является исполнительным слоем.

KAT9I_OS отвечает за исполнение, рабочие процессы и операции.

CKS предоставляет правила, договоры и структуру работы со знаниями.

## Разделение хранения

CKS может содержать адаптеры хранения (модули подключения к хранилищам), но внутреннее состояние CKS не принадлежит KAT9I_OS.

## Основные части

```
CKS
├── core (ядро системы)
├── modes (режимы работы)
├── schemas (схемы данных)
├── protocols (договоры обмена)
├── adapters (адаптеры подключения)
├── knowledge (знания)
├── decisions (решения)
├── graveyard (архив отвергнутых вариантов)
└── evidence (доказательства)
```

## Состояние v1.2

```yaml
architecture: stable
core: frozen
development: gap_driven
```

## Правило изменений

Любое изменение архитектуры проходит путь:

```
GAP (выявленный разрыв)
↓
Evidence (доказательство необходимости)
↓
Proposal (предложение)
↓
Decision (решение)
↓
Minimal Change (минимальное изменение)
```

Предложения и эксперименты не становятся частью архитектуры автоматически.

## Развиваемый рабочий слой после baseline v1.2

Актуальное машинно-читаемое состояние хранится в `control/system-state.yaml`. На 17.09.2026 оно различает:

- baseline `CKS_v1.2`;
- operational `CKS_v1.3`;
- runtime `CKS_v1.4`;
- development `CKS_v1.5`;
- frozen Core;
- evolving Runtime;
- изолированные эксперименты.

Постоянный человеческий Snapshot текущего рабочего слоя: `docs/CKS_CURRENT_WORKING_STATE_SNAPSHOT_2026-09-17.md`.

Постоянная сводка последней подтверждённой интегральной проверки: `reports/CKS_CI_VERIFIED_2026-09-17.md`.

Эти документы предназначены для навигации и фиксации фактического состояния. Они не заменяют Decisions и не повышают рабочие механизмы в Canon автоматически.
