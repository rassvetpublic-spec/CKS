name: Canon Evidence Check

on:
  pull_request:
    paths:
      - '**/*.md'
      - '**/*.yaml'
      - '**/*.yml'

jobs:
  canon-evidence-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check canon evidence boundary
        run: |
          echo "Canon changes require Decision + Evidence review"
          echo "Automation validates only; it does not create Canon"
