name: CKS Release Check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check CKS structure
        run: |
          test -d docs
          test -d schemas
          test -d protocols
