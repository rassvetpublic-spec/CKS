name: CKS Knowledge Check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check CKS structure
        run: |
          test -d docs
          test -d schemas
          test -d protocols
          test -d evidence
          test -d decisions
          test -d graveyard
