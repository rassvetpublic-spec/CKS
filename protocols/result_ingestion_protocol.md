# Result Ingestion Protocol

Agent output enters CKS only as structured artifacts.

Flow:

Result -> Validation -> Evidence check -> Knowledge/Decision candidate

Forbidden:

- raw memory dumps
- hidden agent memory export
- unverified canon updates
