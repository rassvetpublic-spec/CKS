# Agent Memory Boundary

CKS owns validated knowledge.

Agents may use temporary working context but must not become independent knowledge stores.

Boundary:

Agent runtime = temporary execution context
CKS = persistent validated knowledge and decisions

Promotion requires evidence and review.
