# CKS — система контекста и знаний

Независимая система работы с контекстом, знаниями, решениями, доказательствами и историей изменений.

## Статус CKS v1.2

```yaml
version: CKS_v1.2
status: archived
architecture: stable
core: frozen
development: gap_driven
```

> Этот раздел описывает замороженный baseline/Core v1.2. Он не равен полному текущему состоянию развиваемого рабочего слоя. Текущее фактическое состояние зафиксировано в `control/system-state.yaml` и в русском рабочем снимке `docs/CKS_CURRENT_WORKING_STATE_SNAPSHOT_2026-09-17.md`. Рабочий Snapshot не является Canon и не переписывает frozen Core.

## Граница системы

CKS не является исполнительной системой.

CKS отвечает за:

- контекст;
- знания;
- решения;
- доказательства;
- историю изменений.

KAT9I_OS отвечает за исполнение, рабочие процессы и операционные действия.

## Архитектурные части

- Core Engine (ядро системы);
- Context Split Modes (режимы разделения контекста);
- Knowledge Objects (объекты знаний);
- Decision System (система решений);
- Evidence Layer (слой доказательств);
- External Storage Adapters (внешние адаптеры хранения);
- Protocol Contracts (договоры обмена).

## Режимы работы

- SPLIT (разделение);
- AUDIT (аудит);
- REVIEW (проверка);
- RESEARCH (исследование);
- MIGRATION (перенос);
- CLEANUP (очистка);
- MERGE (объединение);
- CANON CHECK (проверка канона).

## Правила развития

1. Расширение ядра запрещено без подтверждённого GAP (выявленного разрыва).
2. Proposal (предложение) не является Decision (решением).
3. Experiment (эксперимент) не является Canon (каноном).
4. Исследовательские материалы не становятся частью архитектуры автоматически.

## Развитие после v1.2

Любое изменение проходит путь:

```
GAP (выявленный разрыв)
↓
Evidence (доказательство)
↓
Proposal (предложение)
↓
Decision (решение)
↓
Minimal Change (минимальное изменение)
```

CKS v1.2 находится в режиме стабильной закрытой архитектуры.

## Текущий рабочий слой

Для текущего рабочего состояния используйте:

- `control/system-state.yaml` — машинно-читаемое состояние версий и слоёв;
- `docs/CKS_CURRENT_WORKING_STATE_SNAPSHOT_2026-09-17.md` — актуальная русская карта реализованного Runtime (рабочего слоя), Knowledge Intelligence (аналитики знаний), графа, Obsidian, федерации и самоаудита;
- `reports/CKS_CI_VERIFIED_2026-09-17.md` — постоянная сводка последней подтверждённой интегральной проверки.

Эти материалы описывают развиваемый рабочий слой и не меняют статус frozen Core v1.2 автоматически.
