# Knowledge Layer

Stores structured knowledge objects.

Rules:

- facts require evidence references;
- decisions are stored separately;
- hypotheses cannot silently become canon;
- every object keeps provenance.
