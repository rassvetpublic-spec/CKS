# SUNO/KAT9I Recon — Checkpoint 006: LUNA body + Sui/KRONOS provenance

Status: **DATA / FORENSICS ONLY — NON-CANONICAL**  
Date: 2026-09-17  
Parent research stream: CKS #31  
Scoped work item: CKS #35

## SSoT boundary

This checkpoint stores research Evidence only. It does not change CKS Canon and does not define the AndreysSUNOBOT music-agent Canon.

- CKS stores Evidence, provenance, candidates and later Decision objects.
- `rassvetpublic-spec/AndreysSUNOBOT` #8/#9 is the durable music-agent/music-engine archaeology and current music-agent canon lane.
- Historical repositories are Evidence sources only.
- No finding below is promoted automatically.
- ABC/XYZ and Genome value remain `UNASSESSED`.

## Freshness anchor

CKS base used for this pass:

`05be6fe3d03d2fe39b2cc911ac0d8ee5c485fea0`

Historical source inspected:

`NewDeep67/KAT9I_IIIJIIOXA@073c497e1bdb64e8341990204d7587a980cc55e0`

Additional negative-evidence tree anchor:

`NewDeep67/KAT9I_IIIJIIOXA@4f939c17b43ee978547bd644872dcea701dba0a9`

## PREV-VERIFY — regression of previous work

The mission remains broader than SUMA/VOX: recover complete agent bodies, embedded libraries, numbered engines, aliases, templates and dependencies.

Previous rules remain valid:

1. `SUMA = VOX` is NOT PROVEN.
2. Registry metadata is not an agent body.
3. A memory filename/allowlist is not proof of its historical content.
4. Current adapted AndreysSUNOBOT role descriptions must not be projected backward without exact provenance.
5. Keyword occurrence is not ownership evidence.

### Regression correction R-006-A

A cached earlier reconnaissance note suggested that the historical LUNA `assets/` directory contained concrete files such as `microdosing_examples.md` and `vocal_tag_blocks.md`.

Fresh exact-ref inspection at `073c497...` does **not** support that claim. The accessible directory currently contains only:

- `assets/README.md`

The README says the directory is intended for research-card templates, search-query presets and databases for LUNA, but it does not itself provide those libraries.

Therefore the earlier concrete asset-filename claim is **REJECTED FOR THIS REF** and must not be used downstream unless recovered from another exact historical ref/blob.

---

## R038 — Full historical LUNA agent body recovered

**Object type:** `AGENT_BODY`  
**Evidence:** `skills-catalog/creative/luna-researcher/SKILL.md` at `073c497...`  
**Historical version:** LUNA Researcher / Music Intelligence Skill v3.5  
**Evidence class:** HISTORICAL-CONFIRMED

The body is substantially richer than a short role label. It defines a complete pre-production research agent.

Recovered knowledge/contract dimensions include:

- track metadata and credits;
- geographic genesis and scene/subculture;
- four-level genre hierarchy: macro family → base genre → regional subgenre → current microgenre;
- BPM, key, swing/pulse character;
- vocal X-ray for VOX and Sui;
- instrumental map for NEO and KRONOS;
- production descriptors;
- trend radar and short-form context;
- team idea bank: take vs reject;
- explicit personal directives to downstream roles.

The body requires factual verification and prohibits invented artist/instrument characteristics.

**Preservation status:** `PARTIAL` relative to current AndreysSUNOBOT: the current system preserves the LUNA role, but historical field-level body equivalence has not yet been demonstrated.

**Disposition:** `PRESERVE_AS_DONOR_REFERENCE`.

---

## R039 — LUNA contains an executable/structured output contract, not only prose

**Object types:** `LIBRARY / TEMPLATE / RULE`  
**Evidence:**

- `skills-catalog/creative/luna-researcher/references/search_queries_and_sources.md`
- `skills-catalog/creative/luna-researcher/scripts/format_luna_dossier.py`
- exact ref `073c497...`

**Evidence class:** HISTORICAL-CONFIRMED

### Search/source reference library

The LUNA reference file defines preferred source classes and query templates for:

- production breakdown;
- BPM/key/credits;
- vocal chain, formant, reverb/delay and delivery analysis;
- genre/microgenre characteristics, BPM and instruments;
- trends/viral sounds;
- URL deconstruction across YouTube, Genius, WhoSampled, Discogs, RYM, SoundCloud and TikTok.

This is direct evidence of a standalone **research-query library**, not merely instructions inside the body.

### Formatter/output schema

`format_luna_dossier.py` materializes a repeatable dossier structure with fields for:

- sound profile: genre, BPM, key, drums, voices, bass, instruments, features/production;
- vocal profile: timbre, effects, flow, speech;
- tangible items, slang and pop references;
- microdosing donor element + Negative barrier;
- role-specific directives for Sui, KRONOS, VANDAL and ZERO;
- source URLs/evidence.

This strengthens the prior conclusion that historical musical knowledge was not only free-text prompt prose: parts of it had structured, executable output contracts.

### Important scope guard

Values in the script's `__main__` demo — artists, BPM, key, genre, vocal settings, slang, negative barrier and other example data — are **fixtures/examples**, not universal agent rules or canonical libraries.

**Preservation status:** `PARTIAL`.

**Disposition:** `PRESERVE_AS_DONOR_REFERENCE`.

---

## R040 — LUNA `assets/` proves an intended library boundary, but concrete library payload is not recovered at this ref

**Object type:** `LIBRARY`  
**Evidence:** `skills-catalog/creative/luna-researcher/assets/README.md@073c497...`  
**Evidence class:** HISTORICAL-CONFIRMED

README intent:

- research-card templates;
- search-query presets;
- databases for LUNA.

But the exact directory listing at this ref exposes only the README itself.

Therefore:

- existence of an **intended assets/library boundary** is confirmed;
- existence/content of specific databases or card/template files at this exact ref is **UNKNOWN**;
- future passes must search older refs/deleted blobs before declaring those assets lost.

**Preservation status:** `UNKNOWN`.

---

## R041 — Suigetsu confirms the ten-role orchestration map, but is not proof of a standalone Sui body

**Object type:** `ENGINE / ORCHESTRATION_FACADE`  
**Evidence:** `skills-catalog/creative/suigetsu-engine/SKILL.md@073c497...`  
**Evidence class:** HISTORICAL-CONFIRMED

The inspected body explicitly names the ten roles:

`LUNA, Sui, KRONOS, VANDAL, Axiom, ZERO, Kodama, VOX, NEO, Mirage`.

It also links the orchestration layer to historical Anti-Slop and Style/Negative materials.

However, this file is named and registered as `suigetsu-engine`; it does not establish that it is the complete standalone body of the role `Sui`.

**Regression correction:** `Suigetsu` and `Sui` must remain separate provenance objects unless an exact alias/version relation is found.

**Preservation status:** `PARTIAL` for orchestration knowledge; standalone Sui body remains `UNKNOWN`.

---

## R042 — KRONOS role is historically consumed, but standalone KRONOS body is not recovered in the inspected slice

**Object type:** `AGENT_BODY / NEGATIVE_EVIDENCE`  
**Positive Evidence:**

- LUNA body routes an instrumental map to `NEO & KRONOS`.
- LUNA formatter includes a dedicated `kronos` directive.
- Suigetsu orchestration names KRONOS as one of the ten roles.

**Negative Evidence:**

- recursive tree inspection around the mature import boundary `4f939c17...` did not recover a path named for KRONOS;
- the inspected historical registry slice at `073c497...` did not recover a KRONOS registry entry by exact name.

This proves neither deletion nor non-existence globally. It means only:

> KRONOS existed as a historically referenced/consumed role, while a standalone KRONOS manifest/body is still unrecovered in the inspected tree/registry slice.

**Preservation status:** `UNKNOWN`.

---

## R043 — Current memory mapping must be reconciled field-by-field, not identity-by-name

The current adapted Agent Registry assigns numbered memory files to LUNA/Sui/KRONOS and the other roles. Historical LUNA evidence now supports many of the *dimensions* represented by that mapping — genre, BPM, voice, instruments, production, artist/persona research and downstream directives.

It still does **not** prove that every current numbered filename existed with the same body, version or ownership in the historical epoch.

Correct migration test:

`historical field/library → exact source → current descendant/owner → preservation status`

Incorrect shortcut:

`same agent name → therefore same complete memory`.

---

## Regression result for Checkpoint 006

### Strengthened

- LUNA is now confirmed as a substantial historical standalone agent body.
- Historical LUNA had real dependencies: a query/source reference library and an executable dossier formatter.
- Historical knowledge included genre/BPM/key/rhythm/instruments/voice/FX/slang/trends and role-routing dimensions.
- The ten-role orchestration map remains supported.

### Corrected

- Concrete LUNA asset filenames previously recalled from cache are not present at exact ref `073c497...`; only an assets README is exposed there.
- `Suigetsu = Sui body` is not supported.
- Current adapted LUNA memory mapping cannot be treated as the historical body wholesale.

### Still open

- standalone Sui body;
- standalone KRONOS body;
- VANDAL, NEO and MIRAGE historical bodies;
- exact historical databases/templates behind LUNA `assets/`;
- numbered-engine map `01..26+`;
- SUMA/VOX lineage beyond already recovered evidence.

## Classification gate

R038–R043 remain:

- `ABC = UNASSESSED`
- `XYZ = UNASSESSED`
- `Genome value = UNASSESSED`

No Canon promotion is performed.

## NEXT SMALL PASS — Checkpoint 007

To keep the context window bounded, the next pass is intentionally limited to one target:

**LUNA library residue only** — search older refs/history for the concrete payload behind `luna-researcher/assets/` and reconcile it with the currently visible README, query-reference file and formatter.

Before that pass: fresh CKS HEAD + regression of R038–R043. Then immediately persist another DATA checkpoint.